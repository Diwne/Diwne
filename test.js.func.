function digaola(name) {
//receba a entrada do usuário e armazene no parâmetro
  name = prompt("Digite o seu nome:")
//crie uma variavel para o olá e o nome armazenado 
  write = "Olá " + name;
//exiba a expressão
  console.log(write)
}
 digaola(name);